## Playground for sk_learn
So far only contains classification with nearest neighbour. Example is a book similarity ranking. #TODO Add features (book summary, genre etc) and turn into recommendation system.
